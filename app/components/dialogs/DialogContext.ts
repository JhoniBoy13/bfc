import {Context, createContext} from "react";

export const DeleteDialogContext: Context<any> = createContext([]);
export const CreateDialogContext: Context<any> = createContext([]);
export const FilterDialogContext: Context<any> = createContext([]);

